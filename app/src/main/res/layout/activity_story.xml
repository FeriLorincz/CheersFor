<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/info_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Please note: If your story contains inappropriate language, a negative tone, or grievances about the company or colleagues, ChatGPT will rewrite your story to ensure it is positive, constructive, and filled with useful insights for readers."
        android:padding="8dp"
        android:background="#f0f0f0"
        android:textColor="#000"
        android:textSize="14sp"
        android:layout_marginBottom="16dp" />

    <EditText
        android:id="@+id/story_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your story..."
        android:layout_below="@id/info_text"
        android:inputType="textMultiLine"
        android:minLines="3"
        android:maxLines="5"
        android:layout_marginTop="8dp" />
    <Button
        android:id="@+id/submit_story_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Submit Story"
        android:layout_below="@id/story_text"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp" />

</RelativeLayout>
